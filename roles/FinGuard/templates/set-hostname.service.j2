[Unit]
Description=Ensure custom hostname
Before=network.target

[Service]
Type=oneshot
ExecStart=/usr/bin/hostnamectl set-hostname {{ hostname }}
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target